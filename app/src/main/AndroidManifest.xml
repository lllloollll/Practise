<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.practise">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="any"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practise"
        tools:replace="android:appComponentFactory">
        <activity
            android:name=".testjs.TestJsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.slide.rvinvp2.TestRvInVp2Activity"
            android:exported="true">
        </activity>

        <receiver
            android:name=".notification.TestNotificationReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".base.BaseActivity"
            android:exported="false" />
        <activity
            android:name=".notification.TestNotificationActivity"
            android:exported="false" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>